# CompatBSD application definition: Spotify
schema_version=1

id=spotify
display_name=Spotify
runtime=ubuntu-24.04
architecture=amd64

source_type=deb
source_url=
source_sha256=

install_path=/opt/spotify
executable=/opt/spotify/spotify

needs_network=yes
needs_x11=yes
needs_dbus=yes
needs_audio=yes
needs_gpu=optional

home_mode=private
sandbox_mode=compatibility
audio_backend=auto
status=planned
