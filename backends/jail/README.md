# Jail backend

The jail backend is intentionally deferred until the Ubuntu runtime and host
bridges work through the chroot backend. This directory records the intended
architectural boundary without pretending that an implementation exists.
